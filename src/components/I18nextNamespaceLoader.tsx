/*import { ReactNode, useEffect, useState } from "react"

interface I18nextNameSpace {
    namespace: string
    children: ReactNode
}

function I18nextNamespaceLoader({namespace, children}:I18nextNameSpace) {
    const [isNamespaceLoaded, setNamespaceLoaded] = useState(false);
    useEffect(() => {

    })

    if (!isNamespaceLoaded) {
        return <div>Loading...</div>;
    }

    return <>{children}</>
}*/